spring:
  data:
    mongodb:
      host: ${host:localhost}
      port: ${port:27017}
      database: ${database:library-accounting}

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG